services:
  homefinder:
    build: .
    container_name: homefinder-app
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped

networks:
  default:
    name: homefinder-network
